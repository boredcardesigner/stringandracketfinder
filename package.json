{
  "name": "srf-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "String & Racket Finder — static front-end + serverless API (Vercel + Neon Postgres)",
  "scripts": {
    "seed": "node db/seed.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.0",
    "@vercel/analytics": "^2.0.1",
    "@vercel/blob": "^0.27.0",
    "resend": "^4.0.0"
  }
}
